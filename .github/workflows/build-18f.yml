name: Create and publish a Docker image for 18F

on:
  push:
    branches: ["18f"]

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest

    # Sets the permissions for the GITHUB_TOKEN
    permissions:
      contents: write
      packages: write
      attestations: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Log into the container registry
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Setup buildx
      - name: Setup buildx
        uses: docker/setup-buildx-action@v3

      # Build and push the image
      - name: Build and push image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: ./
          file: ./Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository }}:18f
          cache-from: |
            type=gha
          cache-to: |
            type=gha,mode=max

      - name: Tag the commit
        run: |
          git tag latest
          git push origin tag 18f

      # Generate artifacte attestation for evidence of how it was built
      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: ghcr.io/${{ github.repository }}
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true
